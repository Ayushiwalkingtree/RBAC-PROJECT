export const APP_CONFIG = {
  name: 'Tenant Access Console',
  drawerWidth: 280,
  defaultRoute: '/dashboard',
  loginRoute: '/login',
} as const;

export const AUTH_CONFIG = {
  tokenType: 'Bearer',
} as const;
